# /tests/e2e — scripted mock-candidate harness

Lands with T2 (text-mode state traversal) and grows through T5 (voice + editor
end-to-end) and T9 (fault injection: STT dropout, LLM timeout, TTS failover).
